# Secret Key (keep this safe in actual .env)
SECRET_KEY=your-actual-secret-key

# Debug mode - set to False in production!
DEBUG=True

# Allowed hosts - separate multiple with commas
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (using SQLite locally; change to PostgreSQL on Render)
DATABASE_URL=sqlite:///db.sqlite3
